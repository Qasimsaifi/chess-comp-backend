meta {
  name: Create a New Game
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/games
  body: json
  auth: inherit
}

body:json {
  {
    "side": "black",
    "name": "new db"
  }
}

settings {
  encodeUrl: true
}
